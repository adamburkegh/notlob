{
  "name": "notlob-dev",
  "private": true,
  "description": "Repo-root TypeScript dev toolchain for the notlob test suite (parallels notlobenv for Python). Not published.",
  "dependencies": {
    "fast-check": "^3.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
